/**
 * enum Player 枚举 玩家
 * @member white 0 白方
 * @member black 1 黑方
 */
export enum Player{
    white,
    black
}

/**
 * interface Position 位置
 * @member x: number 行数
 * @member y: number 列数
 */
export interface Position{
    x: number;
    y: number;
}

